{% extends 'backoffice/layout.html.twig' %}

{% block title %}Modifier un administrateur
{% endblock %}

{% block content %}
    <h1>Modifier un administrateur</h1>

    <p>
        <a href="{{ path('backoffice_adminuser_index', {id:adminuser.id}) }}" class="btn btn-dark my-2 my-sm-0 col-sd-1">Tous les administrateurs</a>
        |
        <a href="{{ path('backoffice_adminuser_add', {id:adminuser.id}) }}" class="btn btn-dark my-2 my-sm-0 col-sd-1">Ajouter un administrateur</a>
    </p>

    {{ include('backoffice/adminuser/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('backoffice_adminuser_index') }}">Retour à la liste</a>

    {{ include('backoffice/adminuser/_delete_form.html.twig') }}
{% endblock %}