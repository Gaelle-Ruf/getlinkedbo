{% extends "backoffice/layout.html.twig" %}

{% block content %}

    <h2>Modification d'un administrateur</h2>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_row(form.firstname, {'attr': {'class':'geronimo'}}) }}
        {{ form_row(form.lastname, {'attr': {'class':'geronimo'}}) }}
    {{ form_end(form) }}
    <button class="btn btn-dark my-2 my-sm-0 col-sd-1">Valider</button>

    <p>
        <a href="{{ path('backoffice_adminuser_index') }}" class="btn btn-dark my-2 my-sm-0 col-sd-1">Retour à la liste</a>
    </p>

{% endblock %}