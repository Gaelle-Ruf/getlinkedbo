{% extends 'backoffice/layout.html.twig' %}

{% block title %}Modifier un style
{% endblock %}

{% block content %}
    <h1>Modifier un style</h1>

    <p>
        <a href="{{ path('backoffice_style_index', {id:style.id}) }}" class="btn btn-dark my-2 my-sm-0 col-sd-1">Tous les styles</a>
        |
        <a href="{{ path('backoffice_style_add', {id:style.id}) }}" class="btn btn-dark my-2 my-sm-0 col-sd-1">Ajouter un style</a>
    </p>

    {{ include('backoffice/style/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('backoffice_style_index') }}">Retour à la liste</a>

    {{ include('backoffice/style/_delete_form.html.twig') }}
{% endblock %}