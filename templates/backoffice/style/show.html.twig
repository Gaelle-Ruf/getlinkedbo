{% extends "backoffice/layout.html.twig" %}

{% block content %}

    <h3>
        Style
        {{ style.name }}
    </h3>

    <p>
        <a href="{{ path('backoffice_style_edit', {id:style.id}) }}">Editer</a>
        |
        <a href="{{ path('backoffice_style_delete', {id:style.id}) }}" onclick="return confirm('Êtes-vous sur de vouloir effectuer cette action ?');">Supprimer</a>
    </p>

    <p>
        <a href="{{ path('backoffice_style_index') }}">Retour à la liste</a>
    </p>

{% endblock %}
